namespace Kanlook.Models;

/// <summary>Which colour scheme the app paints itself in.</summary>
public enum AppTheme
{
    /// <summary>Follow the app colour mode Windows is set to, and change with it.</summary>
    System,
    Light,
    Dark,
}
