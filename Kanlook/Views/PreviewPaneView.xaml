<UserControl x:Class="Kanlook.Views.PreviewPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Kanlook.ViewModels"
             xmlns:beh="clr-namespace:Kanlook.Behaviors"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PreviewPaneViewModel}">

    <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="20,18,12,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Subject}" FontSize="16" FontWeight="Bold"
                           TextWrapping="Wrap" Foreground="{StaticResource TextPrimaryBrush}" />
                <Button Grid.Column="1" Content="✕" Style="{StaticResource FlatButton}" VerticalAlignment="Top"
                        Command="{Binding CloseCommand}" ToolTip="Close preview" />
            </Grid>

            <StackPanel Grid.Row="1" Margin="20,0,20,12">
                <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,2">
                    <Run Text="From: " FontWeight="SemiBold" />
                    <Run Text="{Binding SenderDisplay, Mode=OneWay}" />
                </TextBlock>
                <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,2"
                           Visibility="{Binding ToNames, Converter={StaticResource NullToVisibilityConverter}}">
                    <Run Text="To: " FontWeight="SemiBold" />
                    <Run Text="{Binding ToNames, Mode=OneWay}" />
                </TextBlock>
                <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,2">
                    <Run Text="Received: " FontWeight="SemiBold" />
                    <Run Text="{Binding ReceivedTime, Mode=OneWay, StringFormat='dddd, MMM d, yyyy h:mm tt'}" />
                </TextBlock>

                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="↩ Reply" Style="{StaticResource OutlineButton}" Command="{Binding ReplyCommand}" Margin="0,0,8,0" />
                    <Button Content="↩↩ Reply All" Style="{StaticResource OutlineButton}" Command="{Binding ReplyAllCommand}" Margin="0,0,8,0" />
                    <Button Content="➡ Forward" Style="{StaticResource OutlineButton}" Command="{Binding ForwardCommand}" />
                </StackPanel>

                <ItemsControl ItemsSource="{Binding Attachments}" Margin="0,12,0,0"
                              Visibility="{Binding Attachments.Count, Converter={StaticResource CountToVisibilityConverter}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource AppBgBrush}" BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1" CornerRadius="6" Padding="8,5" Margin="0,0,6,6">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📄" FontSize="11" Margin="0,0,5,0" />
                                    <TextBlock Text="{Binding FileName}" FontSize="11.5"
                                               Foreground="{StaticResource TextPrimaryBrush}" />
                                    <TextBlock Text="{Binding SizeDisplay, StringFormat=' · {0}'}" FontSize="11"
                                               Foreground="{StaticResource TextSecondaryBrush}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,12,0,0" />
            </StackPanel>

            <Grid Grid.Row="2" Margin="0,0,0,0">
                <WebBrowser beh:WebBrowserHtmlBehavior.HtmlContent="{Binding HtmlBody}" />
                <TextBlock Text="Loading…" Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
